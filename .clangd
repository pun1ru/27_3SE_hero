# 27_3SE_hero workspace — 全局 clangd 设置
# 注意：根 .clangd 禁止放 CompilationDatabase 和 CompileFlags.Add！
# hero_down 和 hero_up 各自通过子目录的 .clangd 独立配置编译数据库。
# 这样两个工程的索引完全隔离，不会出现同名函数交叉跳转的问题。

Diagnostics:
  UnusedIncludes: None

Index:
  Background: Build
